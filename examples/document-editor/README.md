# Shared document editor

Create it with `frame create MyEditor --example document-editor`, or run `bun run document-editor /tmp/MyEditor` from this checkout.

The source lives in [the CLI template](../../packages/cli/templates/document-editor) so the same example ships in exported SDKs. See [the editor guide](../../docs/document-editor.md) for platform behavior and checks.
