# Legend Spark logo

`legend-spark.png` is the Legend Spark icon.

The macOS, Windows, and universal starter templates include the same artwork at `assets/icon.png`, referenced by their Expo and desktop configuration. Replace that file to customize a new app’s icon.
